import { groupBy, keys } from "ramda";

// Helper to extract initiatives out of tasks
// export const groupInitiatives = tasks =>
//   groupBy(task => task.initiatives.map(i => i.description), tasks);
